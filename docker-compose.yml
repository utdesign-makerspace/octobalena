version: '2'
volumes:
    octoprint-data:
services:
  octoprint:
    build: ./octoprint
    restart: always
    volumes:
      - 'octoprint-data:/data'
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"
    privileged: true
    environment:
      - UDEV=1
    ports:
      - "80:80"
